class Fingerprint(string nama, string path)
{
    // Attributes
    private readonly string nama = nama;

    private readonly string path = path;

    // Getters
    public string GetNama()
    {
        return nama;
    }

    public string GetPath()
    {
        return path;
    }
}